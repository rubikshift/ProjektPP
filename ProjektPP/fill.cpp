#include "fill.h"

fill::fill(const point* start, const int* colorToSet, const int* colorToChange)
{
	this->setStart(point);
	this->setColor(color);
	this->colorToChange = *colorToChange;
}

void fill::draw(int** img)
{
	int dx = 0, dy = 0;
	bool toStop = false;
	do
	{
	//wypelnianie od kursora	
	}while(toStop == false);
	
}
